将原先的 DuckDuckGo search和  DuckDuckGo mcp、jina search去掉，改为：
（1）优先用 tavily search。密钥存储在.env.local中。
（2）如果 tavily search 失败，再用 exa mcp。
（3）获取到的候选列表网站，直接在网址前面加上 https://r.jina.ai/，获取网站内容。如果获取失败，则fallback用检索结果的摘要。
（4）上一步获取的全文结果或摘要传入后续步骤进行llm分析